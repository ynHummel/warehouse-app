<h1>WareHouse App</h1>

<h2>Boas vindas ao sistema de gestão de estoques</h2>


<h2>Galpões cadastrados:</h2>
<% if user_signed_in? %>
  <%= link_to 'Cadastrar novo galpão', new_warehouse_path  %>
<% end %>

<% @warehouses.each do |w| %>
  <p>
    <%= link_to w.name, warehouse_path(w.id) %>
    <%= w.code %>
  </p>
<% end %>


<% if user_signed_in? %>
  <h2>Buscar galpões</h2>
  <%= form_with url: search_warehouses_path, method: :get do %>
    <%= label_tag :q, 'Busca:' %>
    <%= text_field_tag :q %>
    <%= submit_tag 'Pesquisar' %>
  <% end %>
<% end %>

<hr>

<p><%= link_to 'Cadastrar modelo de produto', new_product_type_path %></p>
<p><%= link_to 'Criar novo kit de produtos', new_product_bundle_path %></p>